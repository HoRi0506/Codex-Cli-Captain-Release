# v1.6.1

- adds run-level `events.jsonl` and folded `run-state.json` projections
- surfaces reducer `next_action` through orchestration snapshots and `foreman_status` / `foreman_activity`
- adds `archive_targets.notebooklm` config/schema defaults without plaintext credentials
- reports NotebookLM archive readiness in `codex-foreman check-install`
- classifies `notebooklm` as an optional archive-export companion MCP
- documents the NotebookLM MCP add, Codex CLI restart, and browser-auth boundary
- adds regression coverage for run-state projection, NotebookLM readiness diagnostics, and compact install output

# v1.5.42

- adds `runtime.worker_poll_interval_ms` to `foreman-config.json` with a default of `90000`
- surfaces `recommended_next_poll_ms` on status-like MCP results while worker fan-in or pending launches are still active
- compacts default MCP structured status payloads by removing repeated task-card role playbooks and helper framing metadata while preserving `debug` output for full inspection
- keeps Korean proceed-to-completion implementation requests on the review-preserving phase chain instead of the document-only route
- updates the `v1.5.42` release-work plan and install/version pointers
- adds regression coverage for runtime polling defaults, compact MCP status metadata, worker polling guidance, and the Korean mutation phase chain

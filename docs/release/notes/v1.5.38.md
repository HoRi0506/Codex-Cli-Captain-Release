# v1.5.38

- normalized document-plus-fix `$cap` natural-language requests onto a review-preserving hidden route so route-owned execution no longer drops the arbiter boundary
- preserved hidden-route execution and review intent in auto-entry trace metadata, including required review intent when the selected hidden route includes arbiter
- kept investigate-first entry behavior while carrying implementation intent forward in budget-path metadata for routes that continue from scout into raider
- added regression coverage for the hidden-route normalization and metadata contract so diagnose-then-fix route chains stay explicit from entry through queued raider and arbiter steps

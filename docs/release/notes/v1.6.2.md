# v1.6.2

- adds repo-scoped NotebookLM archive config defaults: `mode`, `repo_key`, `auto_create_notebook`, and `local_archive_root`
- adds `codex-foreman notebooklm-status` for repo readiness, auth visibility, and honest upload-boundary reporting
- adds `codex-foreman notebooklm-export-session` for repo/date/session local archive bundle generation
- adds MCP tools `foreman_notebooklm_status`, `foreman_notebooklm_prepare_session_export`, and `foreman_notebooklm_record_export_result`
- records local archive preparation and host-observed export results back into run events
- updates README and install docs so NotebookLM setup, auth checks, and repo-scoped archive behavior match the actual 1.6.2 surface

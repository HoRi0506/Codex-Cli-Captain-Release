# v1.5.41

- replaces new-request hidden route selection with host-captain phase composition from request traits and Foreman agent skill contracts
- adds persisted `execution_plan` metadata to auto-entry answer traces, including phase order, owner agents, skill contracts, modifiers, and Foreman MCP's non-LLM substrate role
- keeps legacy workflow route metadata for old-run compatibility while making the phase plan the canonical new-request decision surface
- preserves valid persisted execution plans on run hydration and only recomposes a fallback plan for legacy or incomplete traces
- updates `$cap`, captain, planner, explorer, code specialist, and verifier instructions around phase contracts, evidence handoff, and final captain synthesis
- extends run schema validation and release tests for the new phase-composition trace
- adds the `v1.5.41` release-work plan for the route-catalog-to-agent-skill-composition transition

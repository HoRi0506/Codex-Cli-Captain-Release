# v1.6.3

- adds `progression_mode=drain_until_boundary` to `foreman_orchestrate`
- lets Foreman drain a phase chain across task boundaries until terminal, manual, fan-in, or max-step boundaries
- returns `progression_mode`, `orchestration_loop_stop_reason`, `captain_boundary_reached`, and `host_local_work_allowed` in orchestration results
- updates `$cap` guidance so captain waits on Foreman phase-chain drain instead of doing host-local continuation work
- verifies background `codex exec` execution and verifier launches use the active `foreman-config.json` role `profile`, `model`, `model_reasoning_effort`, and extra `config_entries`
- updates README and install docs for the 1.6.3 install path and phase-chain behavior

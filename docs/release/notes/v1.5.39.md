# v1.5.39

- makes `$cap` front-door route selection a release-gated contract: captain must select the hidden Foreman route before specialist work begins and must keep execution inside that route until a real boundary
- documents the full route catalog, linked specialist chains, and proof expectations for route selection, worker launch, role/model match, mutation or evidence proof, review, fan-in, and host-fallback blocking
- adds workflow status fields for pending specialist launch truth, whether another bounded orchestrate is required, and whether the selected route is sequential ephemeral execution or bounded parallel fan-out
- exposes run retention policy in lifecycle status so hot state, archive candidates, and prune candidates distinguish structured canonical state from operator-facing markdown summaries
- pins install-facing docs to `v1.5.39` tarball commands instead of `latest` placeholders across source and generated release surfaces
- regenerates the install-only release repository README/install/release-note surfaces from the source-of-truth templates for the `v1.5.39` package
- records the 1.6.0 implementation handoff for executable route state machines, `codex exec` worker launching, proof validation, lifecycle cleanup, and retention/prune commands
- excludes NotebookLM or other external memory providers from the 1.5.39 scope so local structured state remains authoritative

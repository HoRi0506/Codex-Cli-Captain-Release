# v1.5.37

- tightened arbiter verification so mutation reviews carry current-run identity, active task identity, run directory, and worker evidence paths into the bounded verifier launch
- prevented verifier prompts from treating stale sibling `.foreman/runs` artifacts or older smoke outputs as evidence for the active mutation smoke
- added regression coverage for a mutation-required task that passes changed-path evidence into review while excluding a stale sibling run
- documented `v1.5.37` as the mutation-smoke evidence-boundary repair after the `v1.5.36` read-only route smoke passed
